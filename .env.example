# FlowLens Supabase 配置示例
# 复制为 .env 并填写自己的 Supabase 项目值
# 注意：只使用 anon public key（受 RLS 保护），绝不要在客户端写入 service_role key

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Task 7 本地 MVP：浏览器/客户端直连 OpenAI 转录接口
# 仅用于本地验证；生产环境应改为后端或 Edge Function 代理，避免暴露 API Key。
EXPO_PUBLIC_OPENAI_API_KEY=
